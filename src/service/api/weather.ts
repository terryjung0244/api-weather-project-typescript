import axios from 'axios';

const url = https://api.openweathermap.org/data/2.5/weather?lat=${lat}&lon=${lon}&appid=${env.REACT_APP_WEATHER_API}

export const getCurrentWeather = async (lat, lon) => {
  try {
  } catch (err) {
    return err;
  }
};
